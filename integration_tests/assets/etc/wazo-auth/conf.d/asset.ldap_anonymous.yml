enabled_backend_plugins:
  ldap_user: true

ldap:
  uri: ldap://slapd
  bind_anonymous: true
  user_base_dn: "dc=wazo-auth,dc=wazo,dc=community"
  user_login_attribute: 'mail'

backend_policies:
  ldap_user: none
