#!/bin/bash

set -e

. /usr/share/debconf/confmodule

db_input high wazo-auth/db-skip || true
db_go || true

db_get wazo-auth/db-skip
db_skip=$RET
if [ "$db_skip" = "false" ] ; then
    db_input high wazo-auth/db-host || true
    db_input high wazo-auth/db-port || true
    db_input high wazo-auth/db-admin-username || true
    db_input high wazo-auth/db-admin-password || true
    db_input high wazo-auth/db-app-name || true
    db_input high wazo-auth/db-app-username || true
    db_input high wazo-auth/db-app-password || true
    db_go || true
fi

db_stop
